allprojects {
    apply plugin: 'idea'
    repositories {
        mavenLocal()
        //mavenCentral()
    }
    tasks.withType(JavaCompile) {
        options.encoding = "UTF-8"
    }
}

subprojects {
    apply plugin: 'java'
    sourceCompatibility  = 1.7
}